set history save on
set history filename ~/.cache/gdb_history
set history remove-duplicates 1

set print pretty on
set pagination off
set confirm off
set verbose off

alias v  = info locals
alias sf = select-frame
alias rf = reverse-finish

# vim: ft=gdb sw=2 sts=2 et
