#compdef config
typeset -A opt_args
local -a _config_cmds

_config_cmds=(
    'edit:edit config list'
    $(dictionary keys "$HOME/.config/shortcuts/config")
)

_describe -t commands 'config files' _config_cmds
return $?
