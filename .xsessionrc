#!/bin/sh

[ -d "$HOME/.local/bin/X11" ] && export PATH="$PATH:$HOME/.local/bin/X11"

remapkeys &
redshift &

case "$DESKTOP_SESSION" in

    i3*)
        ;;

    bspwm)
        setbg &
        compton &
        launch-sxhkd &
        launch-polybar bspwm &
        dunst &
        ;;

esac
